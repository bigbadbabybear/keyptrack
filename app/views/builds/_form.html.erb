<%= form_for(@build) do |f| %>
  <% if @build.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@build.errors.count, "error") %> prohibited this build from being saved:</h2>

      <ul>
      <% @build.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game %>
		<%= f.collection_select :game_id, Game.order("title ASC"), :id, :title %>
  </div>
  <div class="field">
    <%= f.label :platform %>
		<%= f.collection_select :platform_id, Platform.order("os ASC"), :id, :os %>
  </div>
  <div class="field">
    <%= f.label :commit %>
    <%= f.text_field :commit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
